<nav class="bg-gray-800">
  <div data-controller="toggle menu-button">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="flex h-16 items-center justify-between">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <img class="h-8 w-auto" src="https://tailwindui.com/img/logos/mark.svg?color=indigo&shade=500" alt="Play Your Court">
          </div>
          <div class="hidden sm:ml-6 sm:block">
            <div class="flex space-x-4">
            </div>
          </div>
        </div>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex items-center">
            <button type="button" class="relative rounded-full bg-gray-800 p-1 text-gray-400 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800">
              <span class="absolute -inset-1.5"></span>
              <span>What's new</span>
            </button>

            <!-- Profile dropdown -->
            <div class="relative ml-3" data-controller="dropdown">
              <div>
                <button type="button" class="relative flex rounded-full bg-gray-800 text-sm focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" id="user-menu-button" aria-expanded="false" aria-haspopup="true" data-action="dropdown#toggle click@window->dropdown#hide">
                  <span class="absolute -inset-1.5"></span>
                  <span class="sr-only">Open user menu</span>
                  <div class="flex-shrink-0">
                    <div class="user-initials">
                      PYC
                    </div>
                  </div>
                </button>
              </div>

              <div 
                data-dropdown-target="menu"
                class="hidden absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-md bg-white py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none"
                role="menu" 
                aria-orientation="vertical" 
                aria-labelledby="user-menu-button" 
                tabindex="-1"
                data-transition-enter-from="opacity-0 scale-95"
                data-transition-enter-to="opacity-100 scale-100"
                data-transition-leave-from="opacity-100 scale-100"
                data-transition-leave-to="opacity-0 scale-95"
              >
                <%= link_to new_user_registration_path, class: "block px-4 py-2 text-sm text-gray-700" do %>
                  Sign up
                <% end %>
                <%= link_to new_user_session_path, class: "block px-4 py-2 text-sm text-gray-700" do %>
                  Sign in
                <% end %>
              </div>
            </div>
          </div>
        </div>
        <div class="-mr-2 flex sm:hidden">
          <!-- Mobile menu button -->
          <button type="button" 
            data-action='click->toggle#toggle touch->toggle#toggle click->menu-button#toggle touch->menu-button#toggle' 
            class="relative inline-flex items-center justify-center rounded-md p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" 
            aria-controls="mobile-menu" 
            aria-expanded="false"
          >
            <span class="absolute -inset-0.5"></span>
            <span class="sr-only">Open main menu</span>
            <!--
              Icon when menu is closed.

              Menu open: "hidden", Menu closed: "block"
            -->
            <div data-menu-button-target="hamburger">
              <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
              </svg>
            </div>
            <!--
              Icon when menu is open.

              Menu open: "block", Menu closed: "hidden"
            -->
            <div data-menu-button-target="close" class="hidden transition ease-in-out delay-150">
              <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
              </svg>
            </div>
          </button>
        </div>
      </div>
    </div>
    <!-- Mobile menu, show/hide based on menu state. -->
    <div class="hidden" id="mobile-menu" data-toggle-target="toggleable">
      <div class="space-y-1 px-2 pb-3 pt-2">
      </div>
      <div class="border-t border-gray-700 pb-3 pt-4">
        <div class="flex items-center px-5">
          <div class="flex-shrink-0">
            <div class="user-initials">
              PYC
            </div>
          </div>
          <div class="ml-3">
          </div>
          <button type="button" class="relative ml-auto flex-shrink-0 rounded-full bg-gray-800 p-1 text-gray-400 hover:text-white focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800">
            <span class="absolute -inset-1.5"></span>
            <span>What's new</span>
          </button>
        </div>
        <div class="mt-3 space-y-1 px-2">
          <%= link_to new_user_registration_path, class: "block px-4 py-2 text-sm text-white" do %>
            Sign up
          <% end %>
          <%= link_to new_user_session_path, class: "block px-4 py-2 text-sm text-white" do %>
            Sign in
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>

<div data-controller="toggle">
</div>